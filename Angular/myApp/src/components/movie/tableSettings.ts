export interface ITableSettings{
    imageWidth:number;
    borderRadius:number;
    fontSize:number;
}

export let TableSettings:ITableSettings = {
    imageWidth: 60,
    borderRadius: 15,
    fontSize: 20
}